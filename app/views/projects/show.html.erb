
<div>
  <b><%= @technology.title %> <span class="text-muted">project #<%= @project.id %></span></b>
</div>

<div class="border bg-light rounded p-3 mt-3 mb-5">

  <small class="text-muted">
    Project
  </small>
  <div class="border bg-white rounded p-3 mb-2">
    <small>
      <%= @project.title %>
    </small>
  </div>

  <small class="text-muted">
    Body
  </small>
  <div class="border bg-white rounded p-3 mb-2">
    <p>
      <%= @project.body %>
    </p>
  </div>

  <%= link_to "Back to lessons", technology_projects_path(@technology), class: 'btn btn-secondary btn-sm mt-2' %>
</div>
