<h6 class="mt-5">
  Add new question for <span class="text-muted"><%= @technology.title %></span>
</h6>

<div class="shadow-lg mt-5 mb-5 p-5">
  <% if user_signed_in? %>
    <%= form_for ([@technology, @technology.questions.build]) do |f| %>


        <%= f.label :question %>
        <%= f.text_field :question, required: true, class: 'form-control mb-3' %>


      <%= f.label :snippet %>
      <%= f.text_field :snippet, class: 'form-control mb-3' %>

      <%= f.submit class: 'btn btn-primary btn-sm mt-2' %>
      <%= link_to "Back to lessons", technology_questions_path(@technology), class: 'btn btn-secondary btn-sm mt-2' %>
    <% end %>
  <% end %>
</div>
